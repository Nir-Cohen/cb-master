import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#FF42A1",
      contrastText: "#fff",
    },
    secondary: {
      main: "#FB86B6",
      contrastText: "#fff",
    },
  },
});

export default theme;
